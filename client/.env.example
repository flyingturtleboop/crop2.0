# Client environment variables — copy to .env and fill in real values.
# Note: any VITE_ variable is bundled into the client build and is publicly
# visible. Restrict this API key by HTTP referrer/allowed domains in the
# Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
